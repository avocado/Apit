<%= simple_form_for(@upload_file, :html =>{:multipart => true}) do |f| %>
    <%= f.association :upload_folder %>
    <%= f.input :upload, :as=>:file %>
		<%= f.input :link %>
		<%= f.button :submit %>
<% end %>