<h2>Inloggen</h2>

<%= simple_form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
    <%= f.input :email %>
		<%= f.input :password %>
		<%= f.button :submit, :value => "Log in!", :class => 'btn primary' %>
<% end %>